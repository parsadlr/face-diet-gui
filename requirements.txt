# ============================================================
# Face-Diet — single virtual environment (Python 3.10)
# ============================================================
# Python 3.10 is required. Use .python-version (pyenv) or python3.10 -m venv.
# TensorFlow 2.10 and InsightFace require 3.10.
#
# Setup:
#   pyenv local 3.10  # or use python3.10 directly
#   python -m venv venv
#   source venv/bin/activate   # or venv\Scripts\activate on Windows
#   pip install -r requirements.txt
#   pip install whls/insightface-0.7.3-cp310-cp310-win_amd64.whl  # Windows wheel
#
# GPU: use onnxruntime-gpu below. For CPU-only, swap to onnxruntime>=1.16.0.

# --- NumPy (InsightFace requires 1.x) ---
numpy<2

# --- GUI ---
customtkinter>=5.2.0

# --- Data handling ---
pandas>=2.0.0

# --- Image / video ---
opencv-python>=4.8.0
Pillow>=10.0.0

# --- ONNX runtime (InsightFace); GPU or CPU ---
onnxruntime-gpu==1.18.1
# onnxruntime>=1.16.0   # CPU-only: uncomment and comment line above

# TensorFlow 2.10 needs protobuf<3.20; onnx 1.14+ needs >=3.20.2 — use onnx 1.12.x which allows 3.12–3.20
protobuf>=3.9.2,<3.20
onnx>=1.12.0,<1.14

# --- DeepFace (Tab 1 attribute extraction) ---
deepface>=0.0.79
tensorflow==2.10.*

# --- Stage 3: graph clustering ---
scipy>=1.11.0
faiss-cpu>=1.7.0
python-igraph>=0.10.0
leidenalg>=0.10.0
networkx>=3.0
