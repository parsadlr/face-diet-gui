# ============================================================
# GUI + Clustering environment  (requirements_gui.txt)
# Python 3.10 – 3.13 compatible
# ============================================================
# Run once to install:
#   pip install -r requirements_gui.txt

# --- GUI ---
customtkinter>=5.2.0

# --- Data handling ---
pandas>=2.0.0
numpy>=1.24.0          # numpy 2.x is fine here (no insightface constraint)

# --- Image / video ---
opencv-python>=4.8.0
Pillow>=10.0.0

# --- Stage 3: graph clustering (no TensorFlow required) ---
scipy>=1.11.0
faiss-cpu>=1.7.0        # CPU FAISS; for GPU: conda install -c conda-forge faiss-gpu
python-igraph>=0.10.0   # Leiden algorithm
leidenalg>=0.10.0
networkx>=3.0           # Louvain fallback
